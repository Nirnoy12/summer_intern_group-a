# ============================================================
# LMS Backend — Pinned Requirements (Python 3.11 / 3.12)
# Generated: 2026-07-24
# Usage:  pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.3

# --- Database ORM ---
sqlmodel==0.0.22
sqlalchemy==2.0.41
sqlalchemy-cockroachdb==2.0.2

# --- Database Driver ---
psycopg2-binary==2.9.10

# --- HTTP Client (async) ---
httpx==0.28.1

# --- Environment ---
python-dotenv==1.1.0

# --- Auth ---
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# --- YouTube ---
youtube-transcript-api==1.0.3

# --- LLM ---
# Both Ollama (local) and Groq (cloud) are accessed via httpx above.
# No extra SDK is required — we use their REST APIs directly.
